apiVersion: v1
kind: Secret
metadata:
  name: revcart-secrets
  namespace: revcart
type: Opaque
# Values must be base64 encoded.
# Generate with: echo -n 'your-value' | base64
# To apply with real values, use:
#   kubectl create secret generic revcart-secrets \
#     --from-literal=jwt-secret=<your-jwt-secret> \
#     --from-literal=db-password=<your-db-password> \
#     -n revcart --dry-run=client -o yaml | kubectl apply -f -
data:
  jwt-secret: dmVyeUxvbmdTZWNyZXRLZXlGb3JEZXZlbG9wbWVudE9ubHkxMjM0NTY=  
  db-password: YWRtaW4=  
