apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-auth
  namespace: revcart
spec:
  serviceName: postgres-auth-headless
  replicas: 1
  selector:
    matchLabels:
      app: postgres-auth
  template:
    metadata:
      labels:
        app: postgres-auth
    spec:
      containers:
        - name: postgres-auth
          image: postgres:16
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: "auth_db"
            - name: POSTGRES_USER
              valueFrom:
                configMapKeyRef:
                  name: revcart-config
                  key: db-username
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: revcart-secrets
                  key: db-password
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 1Gi
---
# Headless service — gives the pod a stable DNS: postgres-auth-0.postgres-auth-headless
apiVersion: v1
kind: Service
metadata:
  name: postgres-auth-headless
  namespace: revcart
spec:
  clusterIP: None
  selector:
    app: postgres-auth
  ports:
    - port: 5432
      targetPort: 5432
