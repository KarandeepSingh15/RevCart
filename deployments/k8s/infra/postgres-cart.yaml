apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-cart
  namespace: revcart
spec:
  serviceName: postgres-cart-headless
  replicas: 1
  selector:
    matchLabels:
      app: postgres-cart
  template:
    metadata:
      labels:
        app: postgres-cart
    spec:
      containers:
        - name: postgres-cart
          image: postgres:16
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: "cart_db"
            - name: POSTGRES_USER
              valueFrom:
                configMapKeyRef:
                  name: revcart-config
                  key: db-username
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: revcart-secrets
                  key: db-password
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-cart-headless
  namespace: revcart
spec:
  clusterIP: None
  selector:
    app: postgres-cart
  ports:
    - port: 5432
      targetPort: 5432
